<h2>☆社内共有項目一覧☆</h2>
<% @posts.each do |post| %>
	<div class="well">
	<h3><%= post.title %></h3>
	<p><%= post.body %></p>
	<%= link_to "詳細を見る", post_path(post),
				:class => 'btn btn-default' %>
</div>
<% end %>